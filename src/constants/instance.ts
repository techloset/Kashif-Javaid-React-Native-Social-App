import {db} from '../config/Firebase';
export const user = db.collection('Users');
export const Images = db.collection('Images');
export const profile = db.collection('profile');
export const users = db.collection('Users');
